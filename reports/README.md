# reports

Saidas geradas pelo simulador:

- `comparacao.csv` - faltas de pagina por algoritmo (FIFO, LRU, Clock) sob a mesma carga,
  gerado por `--modo comparacao`.
- `comparacao.png` - grafico de barras opcional, gerado por `python tools/plot_faults.py`.
